# Org secret entries for a bottega deployment (issue #9).
# Loaded by the OMP SDK from the agent dir (data/omp-agent); this file is
# the per-deployment template, mounted from config/omp/ by compose.
#
# Every entry here is obfuscated before content leaves the process toward a
# provider and restored before tool execution (mode: obfuscate). Replace the
# placeholders with real values per deployment — never commit real secrets.
- type: plain
  content: "replace-with-org-github-pat"
  mode: obfuscate
  friendlyName: "Org GitHub PAT"
- type: plain
  content: "replace-with-org-slack-token"
  mode: obfuscate
  friendlyName: "Org Slack token"
