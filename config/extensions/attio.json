{
  "schema": "bottega.extension-snapshot.v1",
  "extensionId": "attio",
  "pinnedAt": "2026-08-16T00:00:00.000Z",
  "source": {
    "catalog": "https://integrations.sh/api.json",
    "specId": "attio",
    "vendorOfficial": true,
    "reviewed": true
  },
  "manifest": {
    "id": "attio",
    "label": "Attio",
    "vendor": "Attio",
    "kind": "mcp",
    "mcp": {
      "serverUrl": "https://mcp.attio.com/mcp",
      "transport": "streamable-http"
    },
    "credentialSchema": {
      "type": "oauth"
    },
    "tools": [
      {
        "name": "attio.search_records",
        "tier": "read",
        "description": "Full-text search Attio records (people, companies, deals) by name, email, or domain.",
        "params": [
          { "name": "query", "type": "string", "description": "Search text", "required": true },
          { "name": "objectType", "type": "string", "description": "Object type slug (e.g. people, companies)" }
        ]
      },
      {
        "name": "attio.create_record",
        "tier": "write",
        "description": "Create an Attio record of the given object type with JSON-encoded attribute values.",
        "params": [
          { "name": "objectType", "type": "string", "description": "Object type slug", "required": true },
          { "name": "valuesJson", "type": "string", "description": "JSON object of attribute values", "required": true }
        ]
      },
      {
        "name": "attio.update_record",
        "tier": "write",
        "description": "Update an existing Attio record's attribute values.",
        "params": [
          { "name": "recordId", "type": "string", "description": "Record id", "required": true },
          { "name": "objectType", "type": "string", "description": "Object type slug", "required": true },
          { "name": "valuesJson", "type": "string", "description": "JSON object of attribute values", "required": true }
        ]
      }
    ],
    "domains": ["mcp.attio.com"]
  }
}
